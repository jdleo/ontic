import { PanelCard } from './QueryPanel'

export function MutationPanel() {
  return (
    <PanelCard
      eyebrow="Mutate"
      title="Intervention drafting"
      body="Ontology patch entry, validation feedback, and version creation controls belong in this panel."
    />
  )
}
